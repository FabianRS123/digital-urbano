# --- API ---------------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
httpx>=0.27.0
truststore>=0.9.0        # usa el almacén de certificados del SO (TLS con antivirus/proxy)

# --- Capa agéntica (LangChain / LangGraph) -----------------------------
langchain>=1.4.0
langgraph>=1.2.0
langchain-openrouter>=0.2.8

# --- Motor numérico de referencia (paridad con src/lib/) ---------------
numpy>=1.26.0
